#include <stdio.h>
#include <stdio.h>
#include <math.h>
#include <limits.h>

// subtract one from largest 32-bit interger and print the result
int main() {
	int x = INT_MAX - 1;
	printf("0x%d\n", x);
	return 0;
}
